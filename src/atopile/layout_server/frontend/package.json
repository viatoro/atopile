{
  "name": "pcb-layout-editor",
  "private": true,
  "scripts": {
    "build": "bun run scripts/build.ts",
    "watch": "bun run scripts/build.ts --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "puppeteer": "^24.37.5",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "earcut": "^3.0.0"
  }
}
